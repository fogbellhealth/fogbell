# frozen_string_literal: true

module Fogbell
  # Verifier review packet: one plain-English markdown page per promoted rulebook item,
  # every claim footnoted with its citation, written for a nurse reader.
  module Review
    class Error < StandardError; end
  end
end
