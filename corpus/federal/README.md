# corpus/federal

Federal RAI Manual, MDS 3.0 item sets, CMS specifications.

Files here are git-ignored. Every document must have a row in `../MANIFEST.md` with source URL, version, download date and sha256; the row `key` is the `doc` value used in rulebook citations.
